# Canonical site origin for hreflang/canonical URLs — set to the production domain on deploy
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Site-wide search-engine visibility. Default (unset) = NOINDEX the whole site
# (robots noindex,nofollow on every page + robots.txt Disallow: /) while on the
# temporary Vercel URL. Set to "false" (and redeploy) to RE-ENABLE indexing
# before the real launch. Single switch — see SEO_NOINDEX in src/lib/seo.ts and
# the reversal note in DECISIONS.md.
NEXT_PUBLIC_NOINDEX=

# --- Form relay (server-side; contact + volunteer + partner forms) ---
# All three forms deliver via the foundation's own Telegram bot and SMTP;
# a request succeeds if at least one channel delivers. Real values later.
# Telegram bot relay
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
# Email relay destination + SMTP transport
CONTACT_EMAIL=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# --- Donate page payment links ---
# PayPal and the Monobank jar are integrated in-page (see src/lib/payments.ts —
# public ids/links, no env). LiqPay is optional: set a URL to flip it from a
# "coming soon" stub to an active outbound link.
NEXT_PUBLIC_LIQPAY_URL=
